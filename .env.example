# Generate a random secret: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
AUTH_SECRET=****

# The following keys below are automatically created and
# added to your environment when you deploy on Vercel

# Instructions to create an AI Gateway API key here: https://vercel.com/ai-gateway 
# API key required for non-Vercel deployments
# For Vercel deployments, OIDC tokens are used automatically
# https://vercel.com/ai-gateway
AI_GATEWAY_API_KEY=****


# Instructions to create a Vercel Blob Store here: https://vercel.com/docs/vercel-blob
BLOB_READ_WRITE_TOKEN=****

# PostgreSQL (Supabase, Neon ou outro). Para Supabase: Database → Connection string.
# Ver supabase/ENV-MAPPING.md para mapear variáveis do projeto Supabase.
POSTGRES_URL=****

# Supabase (cliente JS: Auth, Storage, Realtime). Obtenha em: Dashboard → Settings → API
# Com estas variáveis o app usa Supabase Storage para upload de ficheiros (senão usa Vercel Blob).
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=****
SUPABASE_SERVICE_ROLE_KEY=****
# Bucket de Storage para ficheiros do chat (criar no Dashboard → Storage). Opcional; default: chat-files
# SUPABASE_STORAGE_BUCKET=chat-files

# Redis (opcional). Só necessário para rate limiting do chat. Ver: https://vercel.com/docs/redis
# REDIS_URL=****
